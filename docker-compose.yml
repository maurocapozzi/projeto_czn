services:
  web:
    image: nginx:alpine
    container_name: czn-web-dev
    ports:
      - "8088:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    environment:
      - NGINX_PORT=80
    restart: unless-stopped
